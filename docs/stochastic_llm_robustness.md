# Stochastic LLM robustness

Run the same input through the LLM multiple independent times using `n_runs > 1`. The package saves per-run outputs and computes spatial correlation and selected-region overlap.
